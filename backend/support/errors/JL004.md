# JL004 - Classe Pack não encontrada

O Registry não encontrou `class Pack{}` no arquivo principal da biblioteca. Adicione a estrutura ao arquivo com o mesmo nome do pacote:

```jls
info{
    nome: "Math",
    versao: "1.0.0"
}

class Pack{
}
```
