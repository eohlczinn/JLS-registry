# Terminal JLScript

`jls new folder Nome` cria a estrutura de um pacote. `jls compile Nome` compila o arquivo principal da pasta. `jls Nome --version` lê a versão informada no pacote. `jls search nome` pesquisa o Registry. Para publicar, a CLI precisa de autenticação e da URL do Registry configurada.
