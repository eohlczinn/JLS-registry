import {z} from 'zod'; export const publish=z.object({name:z.string().regex(/^[a-zA-Z][\w-]*$/),version:z.string().regex(/^\d+\.\d+\.\d+$/),description:z.string().max(500).default(''),readme:z.string().max(100000).default(''),license:z.string().max(80).default('MIT'),dependencies:z.array(z.string()).default([])}); export const validate=(schema,body)=>{const r=schema.safeParse(body);if(!r.success){const e=new Error(r.error.issues.map(i=>i.message).join('; '));e.status=400;throw e}return r.data};
